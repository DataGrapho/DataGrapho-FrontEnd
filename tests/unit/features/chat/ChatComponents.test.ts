import { describe, expect, it } from 'vitest'

describe('cobertura unitaria dos componentes de chat', () => {
  it('disponibiliza contratos basicos de renderizacao', () => {
    expect(true).toBe(true)
  })
})
