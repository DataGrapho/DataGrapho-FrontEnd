import { describe, expect, it } from 'vitest'

describe('integracao das acoes de resposta do chat', () => {
  it('habilita acoes apos conclusao', () => {
    const completed = true
    expect(completed).toBe(true)
  })
})
