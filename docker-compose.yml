version: '3.8'

services:
  front:
    build: .
    container_name: datagrapho_frontend
    ports:
      - "4173:3000"
    environment:
      - VITE_API_BASE_URL=http://localhost:8000
    volumes:
      - .:/app
      - /app/node_modules
