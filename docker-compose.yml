version: '3.8'

services:
  front:
    build:
      context: .
      args:
        # API acessível via https://pendengas.com.br/api
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://pendengas.com.br/api}
    container_name: datagrapho_frontend
    ports:
      - "3000:80"
    restart: unless-stopped
